/*! Kernel CI Dashboard | Licensed under the GNU GPL v3 (or later) */
define(function() {
    'use strict';
    var appConstants;

    appConstants = {};

    var MAX_DATE_RANGE = 14,
        MAX_QUERY_LIMIT = 1024;

    appConstants,MAX_DATE_RANGE = MAX_DATE_RANGE;
    appConstants.MAX_QUERY_LIMIT = MAX_QUERY_LIMIT;

    return appConstants;
});
