/*! Kernel CI Dashboard | Licensed under the GNU GPL v3 (or later) */
define(function() {
    'use strict';
    var appConstants;

    appConstants = {
        MAX_DATE_RANGE: 14,
        MAX_QUERY_LIMIT: 1024
    };

    return appConstants;
});
